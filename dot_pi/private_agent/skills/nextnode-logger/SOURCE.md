# SOURCE.md

Vendored from `walid-mos/mac-config` (commit `0163257`, "chore(skills): resync nextnode @7185a94 + portage pi"), pi tree `pi/.pi/agent/skills/`.

- Upstream original: `claude/.claude/skills/nextnode-logger/` in the same repo.
- Underlying facts verified against `NextNodeSolutions/core` `packages/logger` @ `7185a94` — no commit touched `packages/logger` between `7185a94` and core HEAD `649362a` (2026-09-20), so the content is current as of import (2026-09-24).
- Import date: 2026-09-24. Update method: diff `packages/logger` against the `synced-at` commit recorded in SKILL.md, re-sync if it moved, then re-copy the upstream folder.
