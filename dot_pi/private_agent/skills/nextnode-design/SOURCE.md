# SOURCE.md

Vendored from `walid-mos/mac-config` (commit `0163257`, "chore(skills): resync nextnode @7185a94 + portage pi"), pi tree `pi/.pi/agent/skills/`.

- Upstream original: `claude/.claude/skills/nextnode-design/` in the same repo.
- Underlying facts verified against `NextNodeSolutions/core` @ `7185a94` (standards v1.22.0) — the design source of truth `nextnode-landing/src/styles/global.css` is unchanged since `22a8e62` (2026-04-12), so the content is current as of import (2026-09-24).
- Import date: 2026-09-24. Update method: re-check `nextnode-landing/src/styles/global.css`, then re-copy the upstream folder.
